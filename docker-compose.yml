version: '3'

services:

  nginx:
    restart: always
    build: ./nginx
    volumes:
      - web-static:/usr/src/app/static
    ports:
      - "80:80"
    links:
      - web:web

  web:
    restart: always
    build: ./web
    expose:
      - 8000
    links:
      - redis:redis
      - postgres:postgres
    volumes:
      - web-static:/usr/src/app/static
    env_file: .env
    command: bash -c "
      cd /usr/src/app &&
      python manage.py collectstatic --noinput --clear &&
      python manage.py migrate &&
      daphne -b 0.0.0.0 -p 8000 quizbowl.asgi:channel_layer"

  worker:
    restart: always
    build: ./web
    env_file: .env
    command: bash -c "cd /usr/src/app && python manage.py runworker"

  redis:
    restart: always
    image: redis:latest
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

  postgres:
    restart: always
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data/

volumes:
  web-static:
  redisdata:
  pgdata: